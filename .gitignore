node_modules/
dist/
.wrangler/
*.log
# corpus.json is a build artifact; regenerated by `npm run build:corpus`.
# Kept out of git so the vendored headers stay the single source of truth.
generated/corpus.json
# NOTE: vendor/ak-inc/ IS committed on purpose — it makes this repo build
# standalone (no firmware checkout). Refresh it with `npm run fetch-headers`.
